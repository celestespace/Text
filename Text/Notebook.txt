运算优先级
占位符

\n 表示换行
\ 转义符
\t 表示一个tab键的空格
\b 退格键  放在字符串两端没有效果
\r\n windows系统不认识\n  只认识\r\n  表示换行
\\  表示一个\  如果\很多  可以在前面加一个@符号  使\单纯的是\符号  取消转义作用
@ 1、取消转义符的转义作用
  2、将字符串按照原格式输出

  强制类型转换 :
  double d = 10;
  int dd = (int)d

  对于 ++ -- 这样只需要一个操作就能完成的运算，称之为一元运算符
  + - * / %  对于这些需要两个或以上才能完成运算的操作符  称之为二元运算符 
  同时存在一元和二元  先算一元


  关系运算符 和 逻辑运算符 
  关系运算符：> < >= <= == !=
  逻辑运算符：&&	与       || 或       ! 非 
  逻辑表达式 的结果 同样也是bool类型


  复合赋值运算符
  +=  -= *=  /=  %=
  +=：  n = n+1


  if-else  
  注意  ： else永远和离他最近的那个if 配对 

  异常捕获
try-catch
try
{
	代码
}
catch
{
	出现异常要执行的代码
}

三元表达式
表达式1？表达式2：表达式3

常量 ：  const 变量类型  变量名  = 变量值；

枚举：
[public] enum 枚举名
{
	值1，
	值2，
	值3，
	············
}
public ：访问修饰符  公开的公共的  哪都可以访问
enum 关键字  生命枚举的关键字
枚举名： 要符合Pascal命名规范

  结构 
  public struct 结构名
  {
  public 变量类型 字段名// 字段前一般都加_  代码规范
  }

  方法：
  public static 返回值类型  方法名（【参数列表】）
  {
		
  }
  返回值类型： 如果不需要写返回值  写void
  方法名：pascal
  参数列表：完成这个方法所必须要提供给这个方法的条件，如果没有参数  小括号也不能省略。
  return  1、饭回要返回的值  2、立即结束方法；